package EMSSystem;

public class firetruck extends Dispatch{

	
	public firetruck()
	{
		
		homelocationX=rand.nextInt(Map.getFireLoc().size());
		homelocationY=rand.nextInt(Map.getFireLoc().get(locationX).size());
		size=2;

		
		active=true;
		
		

	}
}
